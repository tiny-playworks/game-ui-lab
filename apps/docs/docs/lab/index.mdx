import { DocShell } from '../../components/doc-shell';
import { Localized } from '../../components/locale';
import { LabPreview } from '../../components/lab-preview';
import { LiveLabFrame } from '../../components/live-lab-frame';

<DocShell
  eyebrowZh="实验台"
  eyebrowEn="Live Lab"
  titleZh="组合预览"
  titleEn="Playground"
  summaryZh="把多个 primitive 放在同一个 HUD 场景中，检查它们组合后的视觉节奏。"
  summaryEn="Place multiple primitives in one HUD scene to inspect their combined rhythm."
>
  <section className="docs-section">
    <Localized
      as="p"
      className="docs-section__text"
      zh="开发态会在这里嵌入真正的 gallery 实验台；单个组件的 API 请看 Primitives。"
      en="In local development this page embeds the real gallery lab. See Primitives for individual APIs."
    />
    <LiveLabFrame />
  </section>

  <LabPreview />
</DocShell>
