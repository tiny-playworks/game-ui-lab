import { DocShell } from '../../components/doc-shell';
import { Localized } from '../../components/locale';

<DocShell
  eyebrowZh="入门"
  eyebrowEn="Getting Started"
  titleZh="Getting Started"
  titleEn="Getting Started"
  summaryZh="安装包，从公开入口导入组件和静态样式。"
  summaryEn="Install the packages and import components and static styles from the public entry."
>
  <section className="docs-section">
    <Localized
      as="h2"
      className="docs-section__title"
      zh="安装"
      en="Install"
    />
    <pre className="docs-card"><code>pnpm add @tiny-playworks/game-ui @tiny-playworks/tokens</code></pre>
  </section>

  <section className="docs-section">
    <Localized
      as="h2"
      className="docs-section__title"
      zh="用法"
      en="Usage"
    />
    <pre className="docs-demo-block__code"><code>{`import { DamageNumber, GameUiProvider } from '@tiny-playworks/game-ui';
import '@tiny-playworks/game-ui/styles.css';

export function CombatPulse() {
  return (
    <GameUiProvider theme="arcade" tokens={{ accent: '#ff7ad9' }}>
      <DamageNumber value="512" variant="critical" prefix="CRIT" />
    </GameUiProvider>
  );
}`}</code></pre>
    <Localized
      as="p"
      className="docs-section__text"
      zh="样式入口固定为 `@tiny-playworks/game-ui/styles.css`。组件 JS 不再隐式注入样式。"
      en="The fixed style entry is `@tiny-playworks/game-ui/styles.css`. The component JS entry no longer injects styles implicitly."
    />
  </section>

  <section className="docs-section">
    <Localized
      as="h2"
      className="docs-section__title"
      zh="公开入口"
      en="Public Entry"
    />
    <Localized
      as="p"
      className="docs-section__text"
      zh="不要从 `packages/primitives/src/*` 这种内部路径去拿组件。公开入口只有 `@tiny-playworks/game-ui`。"
      en="Do not import from internal paths such as `packages/primitives/src/*`. The public entry is `@tiny-playworks/game-ui`."
    />
  </section>
</DocShell>
