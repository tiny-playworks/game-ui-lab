import { DocShell } from '../../components/doc-shell';
import { Localized } from '../../components/locale';
import { TokenShowcase } from '../../components/token-docs';

<DocShell
  eyebrowZh="令牌"
  eyebrowEn="Tokens"
  titleZh="Design Token 基础层"
  titleEn="Design token foundation"
  summaryZh="Token 是主题、局部覆盖和 Panda recipe 的共同来源。组件样式从独立 CSS 入口加载。"
  summaryEn="Tokens power themes, scoped overrides, and Panda recipes. Component styles are loaded from the explicit CSS entry."
>
  <section className="docs-section">
    <Localized
      as="h2"
      className="docs-section__title"
      zh="直接消费 token"
      en="Direct token usage"
    />
    <pre className="docs-demo-block__code"><code>{`import { gameUiTokens, gameUiTokenVars } from '@tiny-playworks/tokens';
import '@tiny-playworks/tokens/css';`}</code></pre>
  </section>

  <TokenShowcase />
</DocShell>
